---
description:
globs:
alwaysApply: false
---
# MagicOnion服务概念

MagicOnion基于gRPC，提供了多种服务类型：

## 服务类型

### Unary服务

Unary服务是一种请求-响应模式的RPC，类似于普通的Web API调用。

```csharp
// 共享接口定义
public interface IMyService : IService<IMyService>
{
    UnaryResult<int> SumAsync(int x, int y);
}

// 服务端实现
public class MyService : ServiceBase<IMyService>, IMyService
{
    public async UnaryResult<int> SumAsync(int x, int y)
    {
        return x + y;
    }
}
```

### StreamingHub

StreamingHub提供双向实时通信功能，类似于SignalR。客户端可以调用服务端方法，服务端也可以向多个客户端广播消息。

```csharp
// 共享接口定义
public interface IChatHub : IStreamingHub<IChatHub, IChatClient>
{
    Task<string> JoinAsync(string userName);
    Task LeaveAsync();
    Task SendMessageAsync(string message);
}

public interface IChatClient
{
    void OnJoin(string name);
    void OnLeave(string name);
    void OnReceiveMessage(string name, string message);
}
```

## 序列化

MagicOnion支持多种序列化库：

- [MessagePack](mdc:src/MagicOnion.Serialization.MessagePack) - 高性能二进制序列化
- [MemoryPack](mdc:src/MagicOnion.Serialization.MemoryPack) - 更新的高性能二进制序列化

## 服务注册

ASP.NET Core中通过以下方式注册MagicOnion服务：

```csharp
var builder = WebApplication.CreateBuilder(args);
builder.Services.AddMagicOnion(); // 注册MagicOnion服务

var app = builder.Build();
app.MapMagicOnionService(); // 映射MagicOnion服务到路由
```

