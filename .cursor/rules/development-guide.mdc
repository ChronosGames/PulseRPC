---
description:
globs:
alwaysApply: true
---
# 开发指南

## 项目配置
- 所有项目使用中央包版本管理，版本在 [Directory.Packages.props](mdc:Directory.Packages.props) 中定义
- 全局构建属性在 [Directory.Build.props](mdc:Directory.Build.props) 中配置
- NuGet 包源配置在 [NuGet.Config](mdc:NuGet.Config) 中设置

## 开发流程
1. 确保安装了 .NET 9 SDK
2. 使用 Rider 打开 [PulseRPC.sln](mdc:PulseRPC.sln)
3. 运行所有测试以确保环境正确设置
4. 开发新功能时应该：
   - 在 `tests/` 目录下添加相应的测试
   - 在 `samples/` 目录下添加示例代码
   - 在 `docs/` 目录下更新相关文档

## 编码工作流规则

我的编码必须严格遵循以下工作流程：

1. 描述操作计划
   - 在开始编码前，详细描述将要完成的任务
   - 列出任务的细分步骤和具体实现方法
   - 确定可能出现的问题和解决方案

2. 设计单元测试用例
   - 基于功能需求设计全面的测试用例
   - 覆盖预期功能的正常路径和边缘情况
   - 确保为每个函数/方法设计至少一个测试用例

3. 完成实际编码
   - 遵循设计文档和测试用例实现功能
   - 代码必须有良好的注释和文档字符串
   - 遵循代码规范和最佳实践

4. 运行单元测试用例
   - 在完成编码后立即运行测试
   - 修复失败的测试案例
   - 确保所有测试通过

5. 更新文档
   - 更新技术文档以反映代码变更
   - 记录使用说明和API文档
   - 更新开发日志和版本变更说明

请始终按照这个顺序工作，不要跳过任何步骤。针对每个编码任务，生成一个符合这个工作流程的完整计划并按步骤执行。

## 代码生成
- 使用 Source Generator 自动生成客户端代码
- 生成器代码位于 [PulseRPC.Generators](mdc:src/PulseRPC.Generators) 目录

## 性能测试
- 性能测试代码位于 `perf/` 目录
- 运行性能测试前请确保系统处于稳定状态

