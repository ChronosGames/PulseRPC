---
description:
globs:
alwaysApply: false
---
# PulseRPC 架构指南

# PulseRPC 架构设计

## 概述

PulseRPC是一个现代化的高性能RPC框架，采用分层架构设计，支持服务发现、负载均衡、监控和链路追踪等企业级功能。

## 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            应用层 (Application Layer)                        │
├─────────────────────────────────────────────────────────────────────────────┤
│    客户端应用                   │                      服务端应用              │
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Business Logic          │   │   │ Business Services                   │  │
│  │ - Controllers           │   │   │ - Domain Services                   │  │
│  │ - Application Services  │   │   │ - Data Access Layer                 │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────────────────┐
│                            PulseRPC 框架层 (Framework Layer)                 │
├─────────────────────────────────────────────────────────────────────────────┤
│    客户端组件                    │                      服务端组件              │
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ RPC Client              │   │   │ RPC Server                          │  │
│  │ - Proxy Generation      │   │   │ - Service Registration              │  │
│  │ - Request Serialization │   │   │ - Request Handling                  │  │
│  │ - Response Handling     │   │   │ - Response Serialization            │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
│                                │                                            │
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Service Discovery       │   │   │ Service Registration                │  │
│  │ - Service Resolution    │   │   │ - Service Publishing                │  │
│  │ - Health Checking       │   │   │ - Health Reporting                  │  │
│  │ - Endpoint Discovery    │   │   │ - Metadata Management               │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
│                                │                                            │
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Load Balancing          │   │   │ Connection Management               │  │
│  │ - Strategy Selection    │   │   │ - Connection Pool                   │  │
│  │ - Endpoint Weighting    │   │   │ - Session Management                │  │
│  │ - Failover Handling     │   │   │ - Resource Management               │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────────────────┐
│                             基础设施层 (Infrastructure Layer)                 │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Monitoring              │   │   │ Tracing                             │  │
│  │ - Metrics Collection    │   │   │ - Distributed Tracing               │  │
│  │ - Performance Monitor   │   │   │ - Context Propagation               │  │
│  │ - Health Metrics        │   │   │ - Span Management                   │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
│                                │                                            │
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Transport Layer         │   │   │ Configuration                       │  │
│  │ - TCP Communication     │   │   │ - Options Pattern                   │  │
│  │ - Message Framing       │   │   │ - Dependency Injection              │  │
│  │ - Compression           │   │   │ - Logging Integration               │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────────────────┐
│                           外部依赖层 (External Dependencies)                  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐   │   ┌─────────────────────────────────────┐  │
│  │ Service Registries      │   │   │ Monitoring Systems                  │  │
│  │ - Consul                │   │   │ - Prometheus                        │  │
│  │ - Etcd                  │   │   │ - Jaeger                            │  │
│  │ - Zookeeper             │   │   │ - Zipkin                            │  │
│  │ - DNS                   │   │   │ - OpenTelemetry                     │  │
│  └─────────────────────────┘   │   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 核心组件详解

### 1. RPC客户端 (RPC Client)

**职责**: 为客户端应用提供透明的远程服务调用能力

**主要功能**:
- **代理生成**: 使用Source Generator自动生成服务代理类
- **请求序列化**: 将方法调用转换为网络消息
- **响应处理**: 解析服务端响应并返回结果
- **异常处理**: 处理网络异常和业务异常

```csharp
// 自动生成的代理示例
public class UserServiceProxy : IUserService
{
    private readonly IRpcClient _rpcClient;

    public async Task<User> GetUserAsync(int userId)
    {
        var request = new RpcRequest
        {
            ServiceName = "UserService",
            MethodName = "GetUserAsync",
            Parameters = new object[] { userId }
        };

        var response = await _rpcClient.InvokeAsync(request);
        return response.Result.As<User>();
    }
}
```

### 2. RPC服务端 (RPC Server)

**职责**: 接收并处理客户端的RPC请求

**主要功能**:
- **服务注册**: 将本地服务注册到框架
- **请求路由**: 根据请求信息路由到对应的服务方法
- **结果序列化**: 将方法执行结果序列化为响应消息
- **异常封装**: 将异常信息安全地返回给客户端

```csharp
// 服务端处理流程
public class RpcRequestHandler
{
    public async Task<RpcResponse> HandleAsync(RpcRequest request)
    {
        try
        {
            // 1. 解析请求
            var serviceType = ResolveServiceType(request.ServiceName);
            var method = ResolveMethod(serviceType, request.MethodName);

            // 2. 获取服务实例
            var service = _serviceProvider.GetService(serviceType);

            // 3. 执行方法
            var result = await InvokeMethodAsync(service, method, request.Parameters);

            // 4. 返回结果
            return new RpcResponse { Result = result };
        }
        catch (Exception ex)
        {
            return new RpcResponse { Error = ex.ToRpcError() };
        }
    }
}
```

### 3. 服务发现 (Service Discovery)

**职责**: 实现服务的自动发现和管理

**支持的注册中心**:
- **Consul**: 功能完整的服务网格解决方案
- **Etcd**: 分布式键值存储，支持服务发现
- **Zookeeper**: 传统的分布式协调服务
- **DNS**: 基于DNS的简单服务发现

**架构设计**:
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ Service A       │    │ Service B       │    │ Service C       │
│ (Provider)      │    │ (Consumer)      │    │ (Provider)      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                      │                       │
         │ Register             │ Discover              │ Register
         ▼                      ▼                       ▼
┌───────────────────────────────────────────────────────────────┐
│                    Service Registry                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │   Consul    │  │    Etcd     │  │ Zookeeper   │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└───────────────────────────────────────────────────────────────┘
```

### 4. 负载均衡 (Load Balancing)

**职责**: 在多个服务实例之间分配请求

**支持的策略**:
- **轮询 (Round Robin)**: 按顺序轮流分配请求
- **随机 (Random)**: 随机选择服务实例
- **加权轮询 (Weighted Round Robin)**: 根据权重分配请求
- **最少连接 (Least Connections)**: 选择连接数最少的实例
- **一致性哈希 (Consistent Hash)**: 基于请求特征的哈希分配

**实现机制**:
```csharp
public interface ILoadBalancingStrategy
{
    ServiceEndpoint SelectEndpoint(
        IReadOnlyList<ServiceEndpoint> endpoints,
        RpcRequest request);
}

public class WeightedRoundRobinStrategy : ILoadBalancingStrategy
{
    private int _currentIndex = 0;
    private int _currentWeight = 0;

    public ServiceEndpoint SelectEndpoint(
        IReadOnlyList<ServiceEndpoint> endpoints,
        RpcRequest request)
    {
        // 加权轮询算法实现
        return CalculateWeightedEndpoint(endpoints);
    }
}
```

### 5. 健康检查 (Health Check)

**职责**: 监控服务实例的健康状态

**检查类型**:
- **TCP检查**: 检查端口连通性
- **HTTP检查**: 通过HTTP端点检查服务状态
- **自定义检查**: 业务逻辑相关的健康检查

**检查流程**:
```
┌─────────────────┐    检查请求    ┌─────────────────┐
│ Health Monitor  │ ────────────▶ │ Service Instance│
└─────────────────┘               └─────────────────┘
         │                                   │
         │                                   │ 响应
         ▼                                   ▼
┌─────────────────┐    更新状态    ┌─────────────────┐
│ Service Registry│ ◀──────────── │ Health Result   │
└─────────────────┘               └─────────────────┘
```

### 6. 连接池管理 (Connection Pool)

**职责**: 管理客户端到服务端的连接生命周期

**功能特性**:
- **连接复用**: 避免频繁创建和销毁连接
- **连接限制**: 控制单个服务的最大连接数
- **空闲管理**: 自动回收长时间空闲的连接
- **故障恢复**: 自动重连和故障转移

```csharp
public class ConnectionPool
{
    private readonly ConcurrentDictionary<ServiceEndpoint, ConnectionGroup> _connections;
    private readonly ConnectionPoolOptions _options;

    public async Task<IConnection> GetConnectionAsync(ServiceEndpoint endpoint)
    {
        var group = _connections.GetOrAdd(endpoint, ep => new ConnectionGroup(ep, _options));
        return await group.GetConnectionAsync();
    }

    private class ConnectionGroup
    {
        private readonly SemaphoreSlim _semaphore;
        private readonly Queue<IConnection> _availableConnections;

        public async Task<IConnection> GetConnectionAsync()
        {
            await _semaphore.WaitAsync();

            if (_availableConnections.TryDequeue(out var connection) && connection.IsActive)
            {
                return connection;
            }

            return await CreateNewConnectionAsync();
        }
    }
}
```

### 7. 性能监控 (Performance Monitoring)

**职责**: 收集和分析系统性能指标

**监控维度**:
- **系统指标**: CPU、内存、磁盘、网络使用率
- **应用指标**: RPC调用次数、响应时间、错误率
- **业务指标**: 自定义业务相关的指标

**指标类型**:
```csharp
public interface IMetricsCollector
{
    // 计数器 - 单调递增的数值
    void IncrementCounter(string name, IDictionary<string, string>? tags = null);

    // 仪表 - 可上升下降的数值
    void RecordGauge(string name, double value, IDictionary<string, string>? tags = null);

    // 直方图 - 数值分布统计
    void RecordHistogram(string name, double value, IDictionary<string, string>? tags = null);

    // 计时器 - 时间测量
    ITimerContext StartTimer(string name, IDictionary<string, string>? tags = null);
}
```

### 8. 分布式链路追踪 (Distributed Tracing)

**职责**: 跟踪请求在分布式系统中的完整路径

**核心概念**:
- **Trace**: 一次完整的请求链路
- **Span**: 链路中的一个操作单元
- **Context**: 跨服务传播的上下文信息

**追踪流程**:
```
Service A          Service B          Service C
    │                  │                  │
    │ ──── Span 1 ───▶ │                  │
    │                  │ ── Span 2 ─────▶ │
    │                  │                  │ Span 3
    │                  │ ◀── Result ───── │
    │ ◀── Result ───── │                  │
    │                  │                  │

Trace ID: 1234567890abcdef
├── Span 1 (Service A → Service B)
│   ├── Span 2 (Service B → Service C)
│   │   └── Span 3 (Service C internal)
│   └── [结束]
└── [结束]
```

## 数据流设计

### 请求处理流程

```
客户端应用
    │
    ▼
┌─────────────────┐
│ 1. Method Call  │ ─── user.GetUserAsync(123)
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 2. Proxy        │ ─── 代理拦截方法调用
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 3. Serialization│ ─── 序列化请求参数
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 4. Discovery    │ ─── 服务发现获取端点列表
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 5. Load Balance │ ─── 负载均衡选择目标端点
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 6. Transport    │ ─── TCP传输请求数据
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 7. Server Handle│ ─── 服务端处理请求
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 8. Response     │ ─── 返回处理结果
└─────────────────┘
    │
    ▼
客户端应用
```

### 服务注册流程

```
服务实例启动
    │
    ▼
┌─────────────────┐
│ 1. Service Start│ ─── 服务端启动
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 2. Registration │ ─── 注册服务信息到注册中心
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 3. Health Check │ ─── 启动健康检查
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 4. Ready        │ ─── 服务就绪，接受请求
└─────────────────┘
    │
    ▼ (服务运行期间)
┌─────────────────┐
│ 5. Heartbeat    │ ─── 定期发送心跳
└─────────────────┘
    │
    ▼ (服务关闭)
┌─────────────────┐
│ 6. Deregister   │ ─── 注销服务
└─────────────────┘
```

## 可扩展性设计

### 插件架构

PulseRPC采用插件化架构，支持以下扩展点：

1. **传输层插件**: 支持不同的传输协议
2. **序列化插件**: 支持不同的序列化格式
3. **发现插件**: 支持不同的服务注册中心
4. **负载均衡插件**: 支持自定义负载均衡策略
5. **监控插件**: 支持不同的监控系统集成

### 依赖注入集成

框架深度集成.NET依赖注入容器，支持：

- **生命周期管理**: Singleton、Scoped、Transient
- **配置绑定**: Options模式配置管理
- **服务替换**: 支持替换默认实现
- **条件注册**: 基于条件的服务注册

## 性能优化

### 内存管理

- **对象池**: 重用高频创建的对象
- **零拷贝**: 减少不必要的内存复制
- **流式处理**: 支持大数据流式传输

### 网络优化

- **连接复用**: TCP连接池管理
- **批量处理**: 批量发送减少网络开销
- **压缩传输**: 可选的数据压缩

### 并发处理

- **异步编程**: 全异步API设计
- **线程安全**: 线程安全的组件实现
- **背压控制**: 防止系统过载

## 容错设计

### 故障处理

- **重试机制**: 可配置的重试策略
- **熔断器**: 防止故障传播
- **降级策略**: 服务不可用时的降级处理
- **超时控制**: 防止长时间阻塞

### 监控报警

- **健康监控**: 实时健康状态监控
- **性能监控**: 关键性能指标监控
- **异常监控**: 异常和错误监控
- **日志聚合**: 分布式日志收集和分析

这种架构设计确保了PulseRPC框架的高性能、高可用性和良好的可扩展性，能够满足现代微服务架构的各种需求。
