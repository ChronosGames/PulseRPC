using System.Text;

namespace PulseRPC.Generator.Generators;

/// <summary>
/// 生成异步方法辅助代码
/// </summary>
public static class AsyncMethodHelpers
{
    /// <summary>
    /// 生成异步方法包装器的辅助方法
    /// </summary>
    public static string GenerateAsyncHelpers()
    {
        var sb = new StringBuilder();

        sb.AppendLine("// <auto-generated/>");
        sb.AppendLine("// PulseRPC Async Method Helpers");
        sb.AppendLine("#nullable enable");
        sb.AppendLine();
        sb.AppendLine("using System;");
        sb.AppendLine("using System.Threading.Tasks;");
        sb.AppendLine("using System.Diagnostics;");
        sb.AppendLine("using System.Runtime.CompilerServices;");
        sb.AppendLine();
        sb.AppendLine("namespace PulseRPC.Client.Events");
        sb.AppendLine("{");
        sb.AppendLine("    /// <summary>");
        sb.AppendLine("    /// 异步方法辅助扩展 - 为监控包装器提供异步支持");
        sb.AppendLine("    /// </summary>");
        sb.AppendLine("    internal static class AsyncMethodHelpers");
        sb.AppendLine("    {");

        // Task 包装方法
        sb.AppendLine("        [MethodImpl(MethodImplOptions.AggressiveInlining)]");
        sb.AppendLine("        public static async Task CompleteTaskWithMetrics(Task task, string methodName, Stopwatch stopwatch)");
        sb.AppendLine("        {");
        sb.AppendLine("            try");
        sb.AppendLine("            {");
        sb.AppendLine("                await task.ConfigureAwait(false);");
        sb.AppendLine("                // 指标记录将在调用方法中处理");
        sb.AppendLine("            }");
        sb.AppendLine("            finally");
        sb.AppendLine("            {");
        sb.AppendLine("                stopwatch.Stop();");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine();

        // ValueTask 包装方法
        sb.AppendLine("        [MethodImpl(MethodImplOptions.AggressiveInlining)]");
        sb.AppendLine("        public static async ValueTask CompleteValueTaskWithMetrics(ValueTask valueTask, string methodName, Stopwatch stopwatch)");
        sb.AppendLine("        {");
        sb.AppendLine("            try");
        sb.AppendLine("            {");
        sb.AppendLine("                await valueTask.ConfigureAwait(false);");
        sb.AppendLine("            }");
        sb.AppendLine("            finally");
        sb.AppendLine("            {");
        sb.AppendLine("                stopwatch.Stop();");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine();

        // 泛型 Task<T> 包装方法
        sb.AppendLine("        [MethodImpl(MethodImplOptions.AggressiveInlining)]");
        sb.AppendLine("        public static async Task<T> CompleteGenericTaskWithMetrics<T>(Task<T> task, string methodName, Stopwatch stopwatch)");
        sb.AppendLine("        {");
        sb.AppendLine("            try");
        sb.AppendLine("            {");
        sb.AppendLine("                return await task.ConfigureAwait(false);");
        sb.AppendLine("            }");
        sb.AppendLine("            finally");
        sb.AppendLine("            {");
        sb.AppendLine("                stopwatch.Stop();");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine();

        // 泛型 ValueTask<T> 包装方法
        sb.AppendLine("        [MethodImpl(MethodImplOptions.AggressiveInlining)]");
        sb.AppendLine("        public static async ValueTask<T> CompleteGenericValueTaskWithMetrics<T>(ValueTask<T> valueTask, string methodName, Stopwatch stopwatch)");
        sb.AppendLine("        {");
        sb.AppendLine("            try");
        sb.AppendLine("            {");
        sb.AppendLine("                return await valueTask.ConfigureAwait(false);");
        sb.AppendLine("            }");
        sb.AppendLine("            finally");
        sb.AppendLine("            {");
        sb.AppendLine("                stopwatch.Stop();");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine();

        // 重试逻辑
        sb.AppendLine("        public static async Task<T> WithRetryAsync<T>(Func<Task<T>> operation, int maxRetries, TimeSpan delay)");
        sb.AppendLine("        {");
        sb.AppendLine("            Exception? lastException = null;");
        sb.AppendLine("            ");
        sb.AppendLine("            for (int attempt = 0; attempt <= maxRetries; attempt++)");
        sb.AppendLine("            {");
        sb.AppendLine("                try");
        sb.AppendLine("                {");
        sb.AppendLine("                    return await operation().ConfigureAwait(false);");
        sb.AppendLine("                }");
        sb.AppendLine("                catch (Exception ex)");
        sb.AppendLine("                {");
        sb.AppendLine("                    lastException = ex;");
        sb.AppendLine("                    ");
        sb.AppendLine("                    if (attempt == maxRetries)");
        sb.AppendLine("                        break;");
        sb.AppendLine("                    ");
        sb.AppendLine("                    await Task.Delay(delay).ConfigureAwait(false);");
        sb.AppendLine("                }");
        sb.AppendLine("            }");
        sb.AppendLine("            ");
        sb.AppendLine("            throw lastException!;");
        sb.AppendLine("        }");
        sb.AppendLine();

        // 超时包装
        sb.AppendLine("        public static async Task<T> WithTimeoutAsync<T>(Task<T> task, TimeSpan timeout)");
        sb.AppendLine("        {");
        sb.AppendLine("            using var cts = new CancellationTokenSource(timeout);");
        sb.AppendLine("            ");
        sb.AppendLine("            try");
        sb.AppendLine("            {");
        sb.AppendLine("                return await task.WaitAsync(cts.Token).ConfigureAwait(false);");
        sb.AppendLine("            }");
        sb.AppendLine("            catch (OperationCanceledException) when (cts.Token.IsCancellationRequested)");
        sb.AppendLine("            {");
        sb.AppendLine("                throw new TimeoutException($\"Operation timed out after {timeout.TotalMilliseconds}ms\");");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine();

        // 结束类和命名空间
        sb.AppendLine("    }");
        sb.AppendLine("}");

        return sb.ToString();
    }
}