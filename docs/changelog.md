# 变更日志

本文记录 PulseRPC 的用户可见变更。

## 未发布

- 将文档重组为 `docs/getting-started`、`docs/concepts`、`docs/guides`、`docs/reference` 与 `docs/archive`。
- 将 AI Agent 专用操作文档集中到 `.agent`。

## 1.1.5 及更早版本

旧版变更日志文件没有保留历史发布条目。审计旧版本时，以 Git 历史和已发布包元数据为准。
