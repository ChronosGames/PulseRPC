name: "(Legacy) Update pre-built SourceGenerator for Unity"

on:
  workflow_dispatch:

jobs:
  update-sourcegenerator-unity:
    name: Update PulseRPC.Client.SourceGenerator.Unity
    runs-on: ubuntu-latest
    steps:
      - run: echo "Not supported in v7 or later." && exit 1
